# --- Supabase ---
# Safe placeholder values so build/typecheck don’t fail
NEXT_PUBLIC_SUPABASE_URL=https://example.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=public-anon-key
# Service role should never be real in example
SUPABASE_SERVICE_ROLE_KEY=service-role-fake-value

# --- App ---
NEXT_PUBLIC_WEB_BASE_URL=http://localhost:3000

# --- Stripe (test) ---
# Fake/test keys to prevent runtime errors; replace with real ones in .env.local or CI secrets
STRIPE_SECRET_KEY=sk_test_fake_key
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_fake_key
STRIPE_WEBHOOK_SECRET=whsec_fake_value
STRIPE_PRICE_ID=price_fake_value